{
  "type": "mysql",
  "name": "default",
  "host": "localhost",
  "port": 3306,
  "username": "travis",
  "password": "travis",
  "database": "ts_express_boilerplate_test",
  "synchronize": true,
  "logging": false,
  "entities": [
    "dist/api/models/**/*.js"
  ],
  "migrations": [
    "src/migration/**/*.ts"
  ],
  "subscribers": [
    "src/subscribers/**/*.ts"
  ],
  "cli": {
    "migrationsDir": "./dist/migrations",
    "subscribersDir": "./dist/subscribers"
  }
}