const expect = require('chai').expect;
const sinon = require('sinon');

const { EnvironmentConfiguration } = require(process.cwd() + '/dist/api/config/environment.config');

describe('Config', function () {

  describe('Environment', () => {

    before(() => {});

    after(() => {});

    it.skip('EnvironmentConfiguration.load() should failed if version < 14.16.0', (done) => {  
    })

  });

});